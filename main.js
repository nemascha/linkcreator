var addLink = document.getElementById('submit_input');
	addLink.addEventListener('click', createLink);

function createLink() {
	var linksQuantity = document.getElementById('quantity_input').value;

}